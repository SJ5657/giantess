// Shared size multiplier for menu UI (start screen, gender select, pause/esc/game-over,
// inventory, hostage shop, save slots, stat upgrade). 1 = original size.
public static class UiScale
{
    public const float Menu = 0.8f;
}
